import {NextRequest,NextResponse} from 'next/server';import {getSupabaseServerClient} from '@/lib/supabase-server';import {normalizeContent,quoteHash,totalAmount} from '@/lib/quote-utils';import type {QuoteContent} from '@/lib/types';
export const runtime='nodejs';
export async function POST(req:NextRequest){try{const body=await req.json();const c=normalizeContent(body.content as QuoteContent);if(!c.clientId)throw new Error('Client is required');if(!c.salesId)throw new Error('Sales PIC is required');if(!c.directorId)throw new Error('President Director is required');if(!c.items.length)throw new Error('At least one item is required');const hash=quoteHash(c),sb=getSupabaseServerClient();const sourceId=body.sourceQuotationId?String(body.sourceQuotationId):null;
 if(sourceId){const {data:source,error}=await sb.from('quotations').select('*').eq('id',sourceId).single();if(error)throw error;if(source.content_hash===hash)return NextResponse.json(source)}
 const {data:no,error:noErr}=await sb.rpc('allocate_quotation_number',{p_client_code:c.clientCode,p_quote_date:c.quotationDate});if(noErr)throw noErr;const {data,error}=await sb.from('quotations').insert({quotation_no:no,quotation_date:c.quotationDate,client_id:null,client_name:c.clientName,client_code:c.clientCode,sales_pic_id:c.salesId,sales_name:c.salesName,total_amount:totalAmount(c),content_hash:hash,content:c,source_quotation_id:sourceId}).select().single();if(error)throw error;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed to prepare quotation'},{status:400})}}
