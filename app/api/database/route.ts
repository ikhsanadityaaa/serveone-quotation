import {NextRequest,NextResponse} from 'next/server'; import {getSupabaseServerClient} from '@/lib/supabase-server';
export const runtime='nodejs';
const tables:any={clients:'clients',contacts:'client_contacts',sales:'sales_people',directors:'directors'};
function table(entity:string){const t=tables[entity];if(!t)throw new Error('Invalid database entity');return t}
export async function GET(req:NextRequest){try{const e=req.nextUrl.searchParams.get('entity')||'';const sb=getSupabaseServerClient();let q=sb.from(table(e)).select('*'); if(e==='clients')q=q.order('company_name');else q=q.order('name');const {data,error}=await q;if(error)throw error;return NextResponse.json(data||[])}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:400})}}
export async function POST(req:NextRequest){try{const {entity,row}=await req.json();const sb=getSupabaseServerClient();const payload={...row};delete payload.id;if(entity==='clients')payload.client_code=String(payload.client_code||'').trim().toUpperCase();const {data,error}=await sb.from(table(entity)).insert(payload).select().single();if(error)throw error;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:400})}}
export async function PATCH(req:NextRequest){try{const {entity,row}=await req.json();if(!row?.id)throw new Error('Missing id');const sb=getSupabaseServerClient();const payload={...row};delete payload.id;delete payload.created_at;if(entity==='clients')payload.client_code=String(payload.client_code||'').trim().toUpperCase();const {data,error}=await sb.from(table(entity)).update(payload).eq('id',row.id).select().single();if(error)throw error;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:400})}}
export async function DELETE(req:NextRequest){try{const {entity,id}=await req.json();const sb=getSupabaseServerClient();const {error}=await sb.from(table(entity)).delete().eq('id',id);if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:400})}}
