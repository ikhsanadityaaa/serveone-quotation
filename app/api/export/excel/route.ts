import {NextRequest} from 'next/server';import {getSupabaseServerClient} from '@/lib/supabase-server';import {quotationXlsx} from '@/lib/renderers';export const runtime='nodejs';
export async function POST(req:NextRequest){try{const {quotationId}=await req.json();const sb=getSupabaseServerClient();const {data,error}=await sb.from('quotations').select('*').eq('id',quotationId).single();if(error)throw error;const b=await quotationXlsx(data);return new Response(new Uint8Array(b),{headers:{'content-type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','content-disposition':`attachment; filename="quotation.xlsx"`}})}catch(e){return new Response(e instanceof Error?e.message:'Export failed',{status:400})}}
