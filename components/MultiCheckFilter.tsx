'use client';
import {useMemo,useRef,useState,useEffect} from 'react';
export default function MultiCheckFilter({label,values,selected,onChange}:{label:string;values:string[];selected:string[];onChange:(v:string[])=>void}){const[open,setOpen]=useState(false),[q,setQ]=useState('');const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const f=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener('mousedown',f);return()=>document.removeEventListener('mousedown',f)},[]);const opts=useMemo(()=>values.filter(x=>x.toLowerCase().includes(q.toLowerCase())),[values,q]);const toggle=(v:string)=>onChange(selected.includes(v)?selected.filter(x=>x!==v):[...selected,v]);return <div className="filter" ref={ref}><button className="filter-btn" onClick={()=>setOpen(!open)}>{label}{selected.length?` (${selected.length})`:''} ▾</button>{open&&<div className="filter-pop"><input autoFocus placeholder="Search..." value={q} onChange={e=>setQ(e.target.value)}/><div className="filter-list"><label><input type="checkbox" checked={!selected.length} onChange={()=>onChange([])}/><b>(All)</b></label>{opts.map(v=><label key={v}><input type="checkbox" checked={selected.includes(v)} onChange={()=>toggle(v)}/>{v}</label>)}</div></div>}</div>}
